{
  "name": "puppeteer-meeting-bot",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "PUPPETEER_CACHE_DIR=/tmp/puppeteer npx puppeteer browsers install chrome"
  },
  "dependencies": {
    "dotenv": "^17.0.1",
    "express": "^5.1.0",
    "puppeteer": "^24.11.2"
  }
}
